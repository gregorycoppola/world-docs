# Dependency Parsing

## What is Dependency Parsing?

Dependency parsing identifies the grammatical relationships between words in a sentence. Unlike constituency parsing (which builds phrase structure trees), dependency parsing creates a graph where:

- **Nodes** are words
- **Edges** are labeled grammatical relations
- **Arrows** point from heads to dependents

## Example

Sentence: "John gave Mary a book"